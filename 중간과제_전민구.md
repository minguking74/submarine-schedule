# 중간과제 기획서 - SJC2 해저케이블 용량관리 웹앱 + AI 챗봇 구축

- 이름 / 소속: 전민구 / 해저케이블사업팀

## 1. 과제 배경 & 정의 (Problem)
- 해저케이블 시스템(Project A, 이하 "Project A")의 구간별 설계용량, 증설(Lightup) 이력, 영업 파이프라인(Funnel), 고객 계약, 용량 Gap, 수익성 정보를 관리하는 업무를 담당하고 있음.
- 담당자가 여러 명이 함께 이 데이터를 다루면서, 각자 다른 파일/화면을 보고 있어 **데이터 불일치 문제**가 심화되고 있음. 누가 최신 버전을 갖고 있는지, 누가 언제 어떤 값을 바꿨는지 확인이 어려워 의사결정에 쓰이는 숫자에 대한 신뢰도가 떨어짐.
- 이를 해결하기 위해, 여러 사용자가 **하나의 공유 데이터베이스**를 보고 쓰는 정식 웹 애플리케이션을 회사 "Playground" 플랫폼에 구축하고, 나아가 이 데이터를 기반으로 바로 질의응답이 가능한 AI 챗봇까지 갖추고자 함.

## 2. 현황 진단 (As-Is)
- 현재는 Excel 템플릿(더미 기준: `Capacity_Template.xlsx`)으로 시작해, 담당자 간 파일을 **메일/메신저로 주고받으며 수작업으로 취합**하고 있음. 최신본이 무엇인지 헷갈리고, 병합 과정에서 값이 덮어써지거나 누락되는 실수가 발생함.
- 이후 자체적으로 React 기반 단일 HTML 대시보드(더미 기준: `Capacity.html`)를 만들어 사용 중이나,
  - 데이터가 각 사용자의 **브라우저 localStorage**에만 저장되어, 사용자마다/기기마다 보이는 데이터가 서로 다름 (실질적 공유가 안 됨).
  - OneDrive 연동 코드는 있으나 사내 IT 승인(Azure AD 앱 등록)이 지연되어 **비활성 상태**이고, 로그인도 고정 PIN 방식이라 권한 관리가 사실상 없음.
  - 결국 공유는 Excel export/import(복사·붙여넣기)로 이루어져, 반복적인 수작업 시간이 소요됨.
- 잔여 용량, 계약 만료 임박 건, 파이프라인 단계별 현황, 수익성 등을 즉시 확인하고 싶을 때도 여러 탭/파일을 눈으로 뒤져야 답을 찾을 수 있어 **조회 자체가 느림**.

## 3. AI 솔루션 구상 (To-Be)
- 기존 HTML 프로토타입의 화면 구성(Dashboard / Capacity / Funnel / Contracts / Pipeline / Lightup / Gap / 수익성)을 참고하여, Claude Code로 **회사 Playground에 정식 웹 애플리케이션**을 구축한다.
  - 각 사용자의 브라우저에 흩어져 있던 데이터를 하나의 **공유 데이터베이스**로 이전하여, 누가 접속하든 동일한 최신 데이터를 보고 저장하도록 한다.
  - 고정 PIN 대신 최소한의 사용자 구분(뷰어/관리자 등)이 가능한 인증 구조를 적용한다.
- 여기에 더해, **DB에 저장된 데이터 범위 내에서만 답변하는 AI 챗봇**을 추가한다.
  - 대표 질의 유형: 잔여 용량 조회(예: "KR-HK 구간 잔여 용량은?"), 계약/고객 현황 조회(예: "이번 분기 만료 예정 계약은?"), 파이프라인/Funnel 현황 조회(예: "Negotiation 단계인 건 몇 개?"), 수익성/매출 요약(예: "이번 달 Lease MRC 합계는?").
  - 챗봇은 DB에 없는 내용은 추측하지 않고 "데이터에 없음"으로 답하도록 하여, 잘못된 정보로 인한 의사결정 오류를 방지한다.
- 핵심 범위(4일차까지 반드시 되게 할 것 1가지): **웹앱 배포(공유 DB화)와 챗봇 응답 기능이 둘 다 최소한으로라도 동작**하는 것. 완성도보다 두 축이 모두 데모 가능한 수준을 목표로 한다.
